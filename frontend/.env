# Local Development
# During local dev, we use Vite proxy (vite.config.ts) to send /api/* to localhost:5000
VITE_API_URL=https://examproctoringapp.atrealstudios.in

# Production (same domain on Hostinger - leave empty for relative URLs)
# VITE_API_URL=
